apiVersion: v1
kind: Service
metadata:
  name: learn-helm
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  sessionAffinity: None
  selector:
    workload.user.cattle.io/workloadselector: deployment-learn-helm-learn-helm  
  type: ClusterIP